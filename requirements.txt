# Requisitos para instalar la aplicacion y desarrollarla. Un solo archivo:
# ejecucion + pruebas. Instalar con:
#   pip install -r requirements.txt
#
# --- Ejecucion ---
fastapi
uvicorn[standard]
gunicorn
jinja2
python-multipart

sqlalchemy>=2.0
alembic
pymysql
cryptography          # requerido por PyMySQL para auth caching_sha2_password

pydantic>=2
pydantic-settings

argon2-cffi
itsdangerous          # cookie de sesion firmada

pillow
numpy
pytesseract           # requiere el binario tesseract-ocr del sistema
reportlab            # PDF en Python puro: sin cairo ni pango del sistema
pywebpush
python-ulid
tzdata                # zonas IANA: Windows no trae base de zonas horarias

# --- Desarrollo y pruebas ---
pytest
pytest-asyncio
httpx
mypy
ruff
pip-tools
types-pymysql
types-reportlab
pypdfium2            # lee el texto de los PDF generados, para probar su contenido
openpyxl             # lee la hoja de calculo original, para la prueba de fidelidad